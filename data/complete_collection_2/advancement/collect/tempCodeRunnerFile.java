
                sc.nextLine();
            }
            if (sc.hasNextLine()){
                String text = sc.nextLine();
                if (text.contains("ominous_bottle_amplifier"))
                    amplifier.add(text);